{
  "consul": "{{.CONSUL}}:8500",
  "services": [
    {
      "name": "rabbitmq",
      "port": "{{.PORT}}",
      "health": "/usr/bin/curl -o /dev/null --fail -s http://localhost:{{.PORT}}",
      "poll": 3,
      "ttl": 10
    }
  ]
}
